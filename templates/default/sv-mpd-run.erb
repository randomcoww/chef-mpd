#!/bin/sh
mkdir -p <%= @options[:create_directories].join(' ') %>
chown <%= @options[:user] %> <%= @options[:change_owners].join(' ') %>

exec mpd --no-daemon --stdout --stderr <%= @options[:mpd_conf_file] %>
